<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <!-- 检查更新需要联网 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 下载更新与通知 -->
    <uses-permission android:name="android.permission.DOWNLOAD_WITHOUT_NOTIFICATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <queries>
        <package android:name="com.xiaomi.bluetooth" />
        <package android:name="moe.chenxy.oppopods" />
    </queries>

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.HyperVolumeANC">
        <activity
            android:name=".SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="de.robv.android.xposed.category.MODULE_SETTINGS" />
            </intent-filter>
        </activity>
        <activity
            android:name=".OobeActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".InfoActivity"
            android:exported="false"
            android:parentActivityName=".MainActivity" />
        <provider
            android:name=".ModuleConfigProvider"
            android:authorities="io.github.hypervolumeanc.config"
            android:exported="true"
            android:grantUriPermissions="false" />
        <receiver
            android:name=".HookStatusReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="io.github.hypervolumeanc.action.HOOK_ALIVE" />
                <action android:name="io.github.hypervolumeanc.action.PONG" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
